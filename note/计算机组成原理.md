# 计算机组成原理

# 第三章 运算方法与运算器

## 3.1 计算机中的运算

## 3.2 定点加减法运算

### 3.2.1 补码加减法运算方法

加法: [A + B] = [A] + [B]

减法: [A - B] = [A] + [-B] = [A] - [B]

求补运算: [-A] = {[A]}

其中{}是求补运算

### 3.2.2 溢出及检测

#### 1. 单符号

法一:设两数符号位为$f_0$和$f_1$,和符号位$f_s$,则

$$
OF=\overline{f_0}\cdot \overline{f_1} \cdot f_s + f_0 \cdot f_1 \cdot \overline{f_s}
$$

法二:符号位进位为$C_f$, 最高位进位为$C_n$, 则

$$
OF = C_f \oplus C_n
$$

#### 2. 双符号

双符号位最高位永远是正确符号位

01为正溢出,10为负溢出

$$
OF = f_1 \oplus f_2
$$

### 3.2.3 加减法的逻辑实现

带进位链的一位全加器

$S_i = X_i \oplus Y_i \oplus C_i$

$C_{i+1} = X_i Y_i + (X_i \oplus Y_i) C_i$

无符号溢出检测: $UOF = CF = Sub \oplus C_{out}$

引入运算控制位$Sub$, $Input = Y_i \oplus Sub$

利用生成函数与传播函数实现快速加法

$C_4 = G_3 + P_3G_2 + P_3P_2G_1 + P_3P_2P_1G_0 + P_3P_2P_1P_0C_0$

[zy1](/media/zy1.png)

## 3.3 定点乘法运算

### 3.3.1 原码一位乘法

![zy2](/media/zy2.png)

![zy3](/media/zy3.png)

### 3.3.2 补码一位乘法

![zy4](/media/zy4.png)

![zy5](/media/zy5.png)

### 3.3.3 阵列乘法器

![zy6](/media/zy6.png)

![zy7](/media/zy7.png)

### 3.3.4 补码阵列乘法器

![zy8](/media/zy8.png)

![zy9](/media/zy9.png)

### 3.3.5 乘法器性能优化

流水思想：复杂问题分解成细粒度任务并发
核心算法: n个部分和累加

- Booth一位乘 ---> Booth两位乘
- 斜向进位阵列乘法器 ---> 华莱士树乘法器
- 主流乘法器: 二位booth算法 + 华莱士树 + 流水

## 3.4 定点除法运算

### 3.4.1 恢复余数法

![zy10](/media/zy10.png)

### 3.4.2 不恢复余数法

![zy11](/media/zy11.png)

![zy12](/media/zy12.png)

## 3.5 浮点运算

浮点计算步骤： 对阶、尾数运算、规格化、舍入、溢出判断

### 3.5.1 浮点加减法运算

对阶 ：使尾数可以直接相加

规格化: 保证浮点数的编码唯一性

![zy13](/media/zy13.png)

舍入处理:

![zy14](/media/zy14.png)

浮点数加减法五步骤:
- 对阶
- 尾数运算
- 规格化
- 舍入
- 溢出判断

#### 浮点运算举例：求 x + y

*   **x** = $2^{101} \times 0.11011011$
*   **y** = $2^{111} \times (-0.10101100)$
*   **格式要求**:
    *   尾数用补码表示，存储10位，含2位符号位。
    *   阶码用补码表示，双符号位。

##### 0. 将 x, y 转换为浮点格式
首先，确定阶码和尾数的补码表示。
*   **x 的阶码** $E_x = +101_2 = +5_{10}$，其双符号位补码为 `00101`。
*   **x 的尾数** $M_x = +0.11011011$，其双符号位补码为 `00.11011011`。
*   **y 的阶码** $E_y = +111_2 = +7_{10}$，其双符号位补码为 `00111`。
*   **y 的尾数** $M_y = -0.10101100$，其双符号位补码为 `11.01010100` (对 `00.10101100` 取反加一)。

因此，初始浮点表示为：
*   `[x]浮 = 00101, 00.11011011`
*   `[y]浮 = 00111, 11.01010100`

---

##### 步骤 1: 对阶 (Align Exponents)
原则：**小阶向大阶看齐**。

1.  **求阶差**: $\Delta E = E_x - E_y = [E_x]_{补} + [-E_y]_{补}$
    *   $[E_x]_{补} = 00101$
    *   $[-E_y]_{补} = 11001$ (对 `00111` 取反加一)
    *   $\Delta E = 00101 + 11001 = 11110 = -2_{10}$
2.  **判断**: 因为 $\Delta E < 0$，所以 $E_x < E_y$。需要将 x 的阶码增大至与 y 相同。
3.  **操作**: x 的阶码加 2，尾数右移 2 位（算术右移，高位补符号位）。
    *   x 的新阶码: `00101` + `00010` = `00111`
    *   x 的新尾数: `00.11011011` 右移2位 -> `00.00110110`，移出的 `11` 作为**附加位**保留。
4.  **对阶后结果**:
    *   `[x]浮 = 00111, 00.00110110(11)`  (括号内为附加位)
    *   `[y]浮 = 00111, 11.01010100`

---

##### 步骤 2: 尾数求和 (Add Mantissas)
将对阶后的尾数（包含附加位）进行相加。

*   `  00.00110110(11)`
*   `+ 11.01010100(00)`  (y的附加位视为0)
*   -------------------
*   `  11.10001010(11)`

*   **结果**: `[x+y]浮 = 00111, 11.10001010(11)`

---

##### 步骤 3: 结果规格化 (Normalize Result)
检查结果尾数是否为规格化形式。对于补码，规格化数的符号位与最高数值位应相反（即 `00.1xxxx` 或 `11.0xxxx`）。

1.  **判断**: 当前尾数 `11.10001010(11)` 的符号位 (`11`) 与最高数值位 (`1`) 相同，属于**非规格化数**。
2.  **操作**: 需要**左规**。尾数左移 1 位，阶码减 1。
    *   尾数左移1位: `11.10001010(11)` -> `11.00010101(1)` (最高位的`1`移出，最低位由附加位的最高位`1`补充，新的附加位为`1`)
    *   阶码减1: `00111` - `00001` = `00110`
3.  **结果**: `[x+y]浮 = 00110, 11.00010101(1)`

---

##### 步骤 4: 舍入处理 (Rounding)
根据题目要求的舍入方式处理附加位。此处给出两种常见方法：

*   **方法一：0舍1入法 (Round to Nearest)**
    *   附加位为 `1`，需要在尾数末位加 1。
    *   `11.00010101` + `0.00000001` = `11.00010110`
    *   **结果**: `[x+y]浮 = 00110, 11.00010110`

*   **方法二：截去法 (Truncate / Chop)**
    *   直接丢弃附加位。
    *   **结果**: `[x+y]浮 = 00110, 11.00010101`


---

##### 步骤 5: 溢出判断 (Overflow Check)
检查最终结果的阶码是否溢出。

*   最终阶码为 `00110` (+6)，双符号位为 `00`，表示正数且无溢出。
*   **结论**: **无溢出**。

##### 最终结果
以“0舍1入法”为例：
*   **[x+y]浮 = 00110, 11.00010110**
*   转换为真值:
    *   阶码: $+6$
    *   尾数: `11.00010110` (补码) -> 原码 `11.11101010` -> 真值 $-0.11101010_2$
    *   所以，$x+y = 2^{110_2} \times (-0.11101010_2) = 2^6 \times (-0.11101010_2)$

### 3.5.2 浮点乘法运算

- 阶码相加
- 尾数相乘
- 规格化

### 3.5.3 浮点除法运算

- 尾数调整:使被除数尾数小于除数尾数
- 阶码相减
- 尾数相除
- 规格化

## 3.6 运算器

### 3.6.1 算术逻辑单元(ALU)

![zy15](/media/zy15.png)

### 3.6.2 运算器与总线结构

- 单总线结构中的数据通路1

![zy16](/media/zy16.png)

- 单总线结构中的数据通路2

![zy17](/media/zy17.png)

- 双总线结构中的数据通路1

![zy18](/media/zy18.png)

- 双总线结构中的数据通路2

![zy19](/media/zy19.png)

- 三总线结构中的数据通路

![zy20](/media/zy20.png)

总线越多,锁存器越少,时钟周期越短,性能越好

# 第四章 存储系统

## 4.1 存储器概述

- 存储系统分层结构
![zy21](/media/zy21.png)

## 4.2 半导体存储器

### 4.2.1 静态MOS存储器(SRAM)

- 六管SRAM存储器 (SRAM Cell)

![zy22](/media/zy22.png)

- 2114 SRAM内部结构

![zy23](/media/zy23.png)

## 4.3 主存的组织及与CPU的连接

## 4.4 并行主存系统

### 4.4.1 高速存储器

- 增加Cache(行缓冲)
- 采用高速器件提高速度
- 采用双端口存储器
- 增加字长, 单个存储周期存取多个字
- 将主存划分为多个模块, 多模块并行

### 4.4.2 双端口存储器

- 具有两组相互独立的读写控制线路
- 两组读写控制线路可以并行操作
- 端口地址不相同, 无冲突, 并行存取
- 端口地址相同, 产生冲突, 需要仲裁机制

### 4.4.3 多模块存储器

#### 1. 单体多字存储器

- 增加字长, 多模块并行
- 多模块同步, 相当于单个存储体
- 双通道内存(联动模式)

#### 2. 多体多字存储器

- 增加字长, 多模块异步并行
- 双通道内存(非联动模式)

#### 3. 多体单字储存器

- 字长不变, 多体交叉存储器
- 高位多体交叉(顺序), 地位多体交叉(交叉)

## 4.5 高速缓冲存储器——Cache

### 4.5.1 cache工作原理

早期U盘写速度慢，部分内存空间为U盘作写缓冲, 数据写入内存即报告完成

- 可有效提升写性能，改善用户等待体验
- 导致数据不一致性，产生脏数据---还未写入

快存为慢存作写缓冲优化写性能

### 4.5.2 程序局部性

![zy24](/media/zy24.png)

程序局部性————程序仅需访问内存很小一部分空间
- 时间局部性: 程序倾向于访问最近访问过的内存位置
- 空间局部性: 程序倾向于访问与最近访问过的内存位置相邻的内存位置

优化手段
- 预读优化: 空间局部性
- 调度算法: 时间局部性

cache基本思想: 在处理器附近增加一个隐藏的小容量快速存储器
- 对程序员透明(可感知, 无法操控)
- 将经常访问的热数据、即将访问的数据放入cache中, 提高访问速度
- 提高命中率优化读性能


### 4.5.3 cache的基本概念

在处理器附近增加一个隐藏的小容量快速存储器
- 对程序员透明(可感知, 无法操控)
- 将经常访问的热数据、即将访问的数据放入cache中, 提高访问速度
- 提高命中率优化读性能

cache术语

- 命中: CPU访问数据在cache中
- 缺失: CPU访问数据不在cache中, 需要从主存调入cache
- 块: cache与主存交换最小单位
- 行/槽: 标记、标志位、数据块容器
- 命中率: 主存访问中cache命中的比例
- 缺失率: 1 - 命中率
- 命中访问时间: 数据查找时间、cache访问时间、总线传输时间
- 缺失损失: 主存块调用cache, 数据传输到处理器的时间

### 4.5.4 cache的读写流程与关键技术

#### 1. cache的读流程

![zy25](/media/zy25.png)

![zy26](/media/zy26.png)

CPU cache读流程
- COU给出主存地址(块地址、 块内地址)
- 主存块地址为关键字进行遍历查找
- 如相符表示副本在cache中, 命中, 访问cache
- 不相符表示数据不在cache中, 缺失, 访问主存
    - 将数据所在块的副本载入cache(块交换————局部性)
    - 载入副本过程可能引起替换(时间长)
    - 更新查找表, 记录当前数据块地址

![zy29](/media/zy29.png)


#### 2. cache的写流程

![zy27](/media/zy27.png)

![zy28](/media/zy28.png)

CPU cache写流程
- CPU给出主存地址(块地址、 块内地址)
- 以主存块地址为关键字进行遍历查找
- 数据命中
    - 写入数据到cache
    - 根据写策略决定是否写入主存
- 数据缺失
    - 非写分配则直接写入主存
    - 写分配则载入数据块并写入数据到cache

![zy30](/media/zy30.png)

#### 3. cache关键技术

- 数据查找

    - 相联存储器
        - 按内容进行访问(key, value)
        - 以关键字作全局并发比较, 硬件成本高
        - 片外cache用于存放查找表
        - 片内cache存放查找表+数据副本
        - 虚拟存储器中存放段表、页表

    - CPU cache 基本组织方式
        - CPU cache由SRAM构成
        - cache与主存均分为固定大小的数据块, 以块为单位交换数据
        - 相联存储器存放查找表/cache
        - CPU给出的块地址与查找表中某单元相同且有效位为1时表示命中


- 地址映射

    - 全相联
        ![zy33](/media/zy33.png)
        - 块映射灵活, 一对多映射
        - cache满后才会出现块冲突
        - 块冲突概率低, cache利用率高
        - 淘汰算法复杂
        - 命中率高

    - 直接相联

        ![zy31](/media/zy31.png)
        - 映射速度快, 一对一映射, 无须查表
            - 利用索引字段直接对比相应标记位即可
            - 查找表可以和副本一起存放，无需相联存储器
            - 查找时间和全相联一样（一级比较器延迟）
        - 块冲突概率高, cache利用率低
        - 淘汰算法简单, 直接淘汰
        - 命中率低
    - 组相联
        ![zy32](/media/zy32.png)
        - 多个相联存储器共享一个多路比较器
        - 相对于全相联 多路比较器复杂度低

- 替换策略
    - 先进先出(FIFO)
    ![zy34](/media/zy34.png)
    - 最近不经常使用(LFU)
    ![zy35](/media/zy35.png)
    - 最近最少使用(LRU)
    ![zy36](/media/zy36.png)

- 写入策略
    - 写回法
    - 写穿法
    - 写分配
    - 写不分配

### 4.5.5 cache应用

#### cache对存储系统性能的影响

- 读优化
    - 提高命中率优化读性能
    - 预读优化空间局部性

- 写优化
    - 写回策略提升突发写性能

- 负面影响
    - 写回策略引入数据不一致性, 产生脏数据
    - 缓冲区满后，写性能降低

## 4.6 虚拟存储器   