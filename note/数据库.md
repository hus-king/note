# 数据库系统原理

# 第三章 关系数据库标准语言SQL

## 3.1 SQL概述

- 综合统一:集数据定义语言（DDL），数据操纵语言（DML），数据控制语言（DCL）功能于一体。
- 高度非过程化
- 面向集合的操作方式
- 以同一种语法结构提供多种使用方式
- 语言简洁，易学易用————9个动词:(查询)select,(定义)create,drop,alter,(操纵)delete,update,insert,(控制)grant,revoke

## 3.2 数据定义

建立数据库时首先要对数据库中数据的结构及特征进行描述，该过程称为数据定义.

在SQL语言中，关于数据的描述信息以SQL模式（Schema）的形式组织。每个模式包含0到多个对象，这些对象被称为模式对象，如基表、视图、索引等。

SQL模式语句可分为三类：CREATE语句、DROP语句和ALTER语句。

### 表：SQL 的数据定义语句

| 操作对象 | 创建 (CREATE) | 删除 (DROP) | 修改 (ALTER) |
| :--- | :--- | :--- | :--- |
| **模式 (SCHEMA)** | `CREATE SCHEMA` | `DROP SCHEMA` | |
| **表 (TABLE)** | `CREATE TABLE` | `DROP TABLE` | `ALTER TABLE` |
| **视图 (VIEW)** | `CREATE VIEW` | `DROP VIEW` | |
| **索引 (INDEX)** | `CREATE INDEX` | `DROP INDEX` | `ALTER INDEX` |

### 3.2.1 模式的定义与删除

#### 1. 模式的定义

模式定义语句如下:

```sql
CREATE SCHEMA <模式名> AUTHORIZATION <用户名>
    [<表定义子句>|<视图定义子句>|<授权定义子句>];
```

语义: 为某用户创建一个模式。定义模式实际上定义了一个命名空间，其中可以定义该模式包含的数据库对象，例如基本表、视图、索引等。

#### 2. 模式的删除

模式删除语句如下:

```sql
DROP SCHEMA <模式名> <RESTRICT|CASCADE>;
```

语义: 删除一个模式。删除模式时，如果该模式中存在对象，则必须指定删除方式：<RESTRICT> 或 <CASCADE>
- <RESTRICT>(限制): 如果模式中存在对象，则拒绝删除模式。
- <CASCADE>(级联): 删除模式及其包含的所有对象。

### 3.2.2 基本表的定义、删除与修改

#### 1. 基本表的定义

基本表定义语句如下:

```sql
CREATE TABLE <表名> (
    <列名><数据类型>[列级完整性约束条件]
    [,<列名><数据类型>[列级完整性约束条件]]
    [,PRIMARY KEY(列名[,列名]...)]
    [,FOREIGN KEY(列名[,列名]...) REFERENCES <表名>(列名[,列名]...)]
    [,UNIQUE(列名[,列名]...)]
    [,CHECK(<条件>)]
    ...
    [,<表级完整性约束条件>]
);
```

语义: 定义一个基本表。定义基本表时，必须指定表名和至少一个列定义。每个列定义包括列名、数据类型和可选的列级完整性约束条件。还可以指定表级完整性约束条件。

- <表名>: 所要定义的基本表的名字
- <列名>: 组成该表的各个属性（列）
- <列级完整性约束条件>：涉及相应属性列的完整性约束条件
- <表级完整性约束条件>：涉及一个或多个属性列的完整性约束条件
- PRIMARY KEY: 主键约束，指定一个或多个列作为主键
- FOREIGN KEY: 外键约束，指定一个或多个列作为外键，并引用另一个表的主键
- UNIQUE: 唯一约束，指定一个或多个列的值必须唯一
- CHECK: 检查约束，指定一个条件，要求列的值必须满足该条件

例子:
```sql
CREATE TABLE Course (
    Cno CHAR(4) PRIMARY KEY,          -- 课程号：主码，固定长度4字符
    Cname CHAR(40) NOT NULL,          -- 课程名：非空，固定长度40字符
    Cpno CHAR(4),                     -- 先修课号：允许为空（即没有先修课）
    Ccredit SMALLINT,                 -- 学分：使用小整数类型
    
    /* 表级完整性约束：
       定义外码 Cpno 参照本表 Course 的主码 Cno。
       这是一种“自引用”结构，表示先修课必须是课程表中已有的课程。
    */
    FOREIGN KEY (Cpno) REFERENCES Course(Cno)
);
```
#### 2. 数据类型

##### SQL92 数据类型及其含义

| 数据类型 | SQL92 数据类型 - 含义 |
| :--- | :--- |
| **CHAR(n)** | 长度为 $n$ 的定长字符串 |
| **VARCHAR(n)** | 最大长度为 $n$ 的变长字符串 |
| **INT** | 长整数（也可以写作 INTEGER），范围：$-2^{31} \sim 2^{31} - 1$ |
| **SMALLINT** | 短整数，范围：$-2^{15} \sim 2^{15} - 1$，占用 2 字节 |
| **NUMERIC(p,d)** | 定点数，由 $p$ 位数字（不包括符号、小数点）组成，小数点后面有 $d$ 位数字 |
| **REAL** | 取决于机器精度的单精度浮点数 |
| **Double Precision** | 取决于机器精度的双精度浮点数，占用 8 字节 |
| **FLOAT(n)** | 可选精度的浮点数，精度至少为 $n$ 位数字，占用 4 字节 |
| **DATE** | 日期，包含年、月、日，格式为 `YYYY-MM-DD` |
| **TIME** | 时间，包含一日的时、分、秒，格式为 `HH:MM:SS` |
| **INTERVAL** | 两个 date 或 time 类型数据之间的差 |

#### 3. 模式与表

每一个基本表都属于某一个模式
一个模式包含多个基本表
定义基本表所属模式（常用方法2种）：
- 在表名中明显地给出模式名，如`Create table “S-T”
.Student（......）;`
- 在创建模式语句中同时创建表，如
```sql
CREATE database School;
use School;
CREATE TABLE Student (...);
```

#### 4. 修改数据表

一般格式：
```sql
ALTER TABLE <表名>
    [ ADD <新列名> <数据类型> [ 完整性约束 ] ]
    [ ADD <表级完整性约束> ]
    [ DROP [COLUMN] <列名> [CASCADE｜RESTRICT]]
    [ DROP CONSTRAINT<完整性约束名>[CASCADE｜RESTRICT] ]
    [ ALTER COLUMN<列名> TYPE <数据类型> ]
    [ RENAME COLUMN <列名> TO <新列名>]；
```
语义: 修改一个基本表的结构。可以添加或删除列，添加或删除完整性约束，修改列的数据类型，重命名列等。
- ADD: 添加新列或表级完整性约束
- DROP: 删除列或完整性约束
- ALTER COLUMN: 修改列的数据类型
- RENAME COLUMN: 重命名列

#### 5. 删除数据表

```sql
DROP TABLE <表名> [RESTRICT|CASCADE];
```

### 3.2.3 索引的建立与删除

#### 1. 索引的建立

```sql
CREATE [UNIQUE] [CLUSTER] INDEX <索引名>
    ON <表名>(<列名>[<次序>][,<列名>[<次序>] ]…)；
```

其中，<次序> 可以是 ASC（升序）或 DESC（降序），默认是 ASC。

功能: 在<表名>的表上，对其中的指定列，建立一个名为<索引名>的索引文件。

作用: 提高查询速度

说明:
- UNIQUE(单一索引): 指定索引列的值必须唯一
- CLUSTER(聚集索引): 索引项的顺序与表中记录的物理顺序一致。表中如果有多个记录在索引字段上相同，这些记录构成一簇，只有一个索引值。

#### 2. 索引的修改

```sql
ALTER INDEX <索引名> RENAME TO <新索引名>;
```

#### 3. 索引的删除

```sql
DROP INDEX <索引名>；
或
DROP INDEX <索引名> ON <表名>；
```

## 3.3 数据查询

select 语句的基本格式如下:

```sql
SELECT [ALL | DISTINCT] <目标列表达式> [别名] [, <目标列表达式> [别名]] ...
FROM <表名或视图名> [别名] [, <表名或视图名> [别名]] ... | (<SELECT 语句>) [AS] <别名>
[WHERE <条件表达式>]
[GROUP BY <列名1> [HAVING <条件表达式>]]
[ORDER BY <列名2> [ASC | DESC]]
[LIMIT <行数1> [OFFSET <行数2>]];
```

SELECT语句的含义是,根据WHERE子句的条件表达式从FROM子句中指定的基本表、视图或派生表中找出满足条件的元组, 再按SELECT子句中的目标列表达式选出元组中的属性值形成结果表.

GROUP BY: 将结果按<列名1>的值进行分组,该属性列值相等的元组为一个组,通常会在每组中作用聚集函数.
ORDER BY: 按<列名2>的值对结果进行排序,默认是升序(ASC),也可以指定为降序(DESC).
LIMIT: 限制结果表中的行数,如果指定了OFFSET子句,则从结果表的第<行数2>行开始返回<行数1>行数据.

### 3.3.1 单表查询

#### 1. 选择表中的若干列

(1) 查询指定列

```sql
SELECT Sno, Sname FROM Student;
```

从Student表中取出一个元组, 取出该元组在属性Sno和Sname上的值, 形成一个新的元组.

(2) 查询所有列

```sql
SELECT * FROM Student;
```

(3) 查询经过计算的值

```sql
SELECT Sname, (extract(year from current_date) - extract(year from Sbirthday)) AS Sage
FROM Student;
```
计算当前日期与学生出生日期之间的年数差, 作为学生的年龄, 并给该列取别名Sage.

#### 2. 选择表中的若干元组

(1) 消除取值重复的行

```sql
SELECT DISTINCT Sdept 
FROM Student;
```

(2) 选择满足条件的行

通过WHERE子句指定条件表达式, 选择满足条件的行.

| 查询条件             | 谓词                                                                 |
|----------------------|----------------------------------------------------------------------|
| 比较                 | `=`, `>`, `<`, `>=`, `<=`, `!=`, `<>`, `!>`, `!<`; `NOT` + 上述比较运算符 |
| 确定范围             | `BETWEEN AND`, `NOT BETWEEN AND`                                     |
| 确定集合             | `IN`, `NOT IN`                                                       |
| 字符匹配             | `LIKE`, `NOT LIKE`                                                   |
| 空值                 | `IS NULL`, `IS NOT NULL`                                             |
| 多重条件（逻辑运算） | `AND`, `OR`, `NOT`                                                   |

```sql
SELECT Sname, Sno, Ssex
FROM Student
WHERE Sname LIKE '刘%';
```

```sql
SELECT Cname, Cno
FROM Course
WHERE Cno LIKE '81__6';
/* 注意课程关系中课程号为固定长度，占5个字符大小 */
```

```sql
SELECT Sname, Sno, Ssex
FROM Student
WHERE Sname NOT LIKE '刘%';
```

```sql
SELECT *
FROM Product
WHERE description LIKE '%\_%' ESCAPE '\';
```

#### 3. ORDER BY子句的使用

```sql
SELECT Sno, Grade
FROM SC
WHERE Cno = '81003'
ORDER BY Grade DESC;
```

#### 4. 聚集函数

| 聚合函数                          | 功能说明                                     |
|-----------------------------------|----------------------------------------------|
| `COUNT(*)`                        | 统计元组个数（即行数）                       |
| `COUNT([DISTINCT \| ALL] <列名>)` | 统计一列中值的个数（默认 ALL，含重复；DISTINCT 去重） |
| `SUM([DISTINCT \| ALL] <列名>)`   | 计算一列值的总和（此列必须是数值型）         |
| `AVG([DISTINCT \| ALL] <列名>)`   | 计算一列值的平均值（此列必须是数值型）       |
| `MAX([DISTINCT \| ALL] <列名>)`   | 求一列值中的最大值                           |
| `MIN([DISTINCT \| ALL] <列名>)`   | 求一列值中的最小值                           |

当遇到空值时, 聚集函数中除了COUNT(*)以外, 其他聚集函数会忽略空值。

```sql
SELECT COUNT(DISTINCT Sno)
FROM SC;
```

#### 5. GROUP BY子句

GROUP BY 子句将查询结果按某一列或多列的值分组，值相等的为一组，如果是多列，则先对第一列的值分组，然后对每一组中的值按照第二列值分组，以此类推。结果集中如果有重复的列组值，则将其合并为一行输出。

```sql
SELECT Cno, COUNT(Sno)
FROM SC
GROUP BY Cno;
```

查询在 20192 学期选课超过 10 门的学生学号
```sql
SELECT Sno
FROM SC
WHERE Semester = '20192'
GROUP BY Sno
HAVING COUNT(*) > 10;
```

#### 6. LIMIT子句

```sql
SELECT Sno, AVG(Grade)
FROM SC
GROUP BY Sno
ORDER BY AVG(Grade) DESC
LIMIT 5 OFFSET 2;
/* 取 5 行数据，忽略前 2 行，之后为查询结果数据 */
```

### 3.3.2 连接查询

若一个查询同时涉及两个以上的表，则称之为连接查询.

#### 1. 等值与非等值连接查询

```sql
SELECT Student.*, SC.*
FROM Student, SC
WHERE Student.Sno = SC.Sno;
/* 将 Student 与 SC 中同一学生的元组连接起来 */
```

#### 2. 自然连接查询

把结果表目标列中重复的属性列去掉的等值连接查询则为自然连接查询

```sql
SELECT Student.Sno, Sname, Ssex, Sbirthdate, Smajor, Cno, Grade
FROM Student, SC
WHERE Student.Sno = SC.Sno;
```

#### 3. 符合条件连接查询

```sql
SELECT Student.Sno, Sname
FROM Student, SC
WHERE Student.Sno = SC.Sno 
  AND SC.Cno = '81002' 
  AND SC.Grade > 90;
/* 连接谓词 */        /* 其他选择条件 */
```

#### 4. 自身连接查询

连接操作不仅可以在两个表之间进行，也可以是一个表与其自身进行连接，此时的连接查询称为表的自身连接查询。

```sql
SELECT FIRST.Cno, SECOND.Cpno
FROM Course FIRST, Course SECOND
WHERE FIRST.Cpno = SECOND.Cno 
  AND SECOND.Cpno IS NOT NULL;
```

#### 5. 外连接查询

```sql
SELECT Student.Sno, Sname, Ssex, Sbirthdate, Smajor, Cno, Grade
FROM Student LEFT OUTER JOIN SC ON (Student.Sno = SC.Sno);
```

#### 6. 多表连接查询

```sql
SELECT Student.Sno, Sname, Cname, Grade
FROM Student, SC, Course
WHERE Student.Sno = SC.Sno 
  AND SC.Cno = Course.Cno;
```

### 3.3.3 嵌套查询

查询选修了课程号为 '81003' 的学生姓名
```sql
SELECT Sname                          /* 外层查询或父查询*/
FROM Student
WHERE Sno IN (
    SELECT Sno                        /* 内层查询或子查询*/
    FROM SC
    WHERE Cno = '81003'
);
```

#### 1. 带有IN谓词的子查询

```sql
SELECT Sno, Sname, Smajor        
FROM Student
WHERE Smajor IN (
    SELECT Smajor
    FROM Student
    WHERE Sname = '刘晨'
);
```

#### 2. 带有比较运算符的子查询

```sql
SELECT Sno, Sname, Smajor
FROM Student
WHERE Smajor = (
    SELECT Smajor
    FROM Student
    WHERE Sname = '刘晨'
);
```
找出每个学生超过他自己选修课程平均成绩的课程号
```sql
SELECT Sno, Cno
FROM SC x
WHERE Grade >= (
    SELECT AVG(Grade)
    FROM SC y
    WHERE y.Sno = x.Sno
);
```

#### 3. 带有 ANY（SOME）或ALL 谓词的子查询

```sql
SELECT Sname, Sbirthdate, Smajor
FROM Student
WHERE Sbirthdate > ANY (
    SELECT Sbirthdate
    FROM Student
    WHERE Smajor = '计算机科学与技术'
)
AND Smajor <> '计算机科学与技术';     /* 注意这是父查询块中的条件 */
```

#### 4. 带有EXISTS谓词的子查询
查询选修了课程号为 '81001' 的学生姓名 —— 使用 EXISTS 相关子查询

```sql
SELECT Sname
FROM Student
WHERE EXISTS (
    SELECT *
    FROM SC
    WHERE Sno = Student.Sno AND Cno = '81001'
);
```

### 3.3.4 集合查询

集合操作主要包括并操作 UNION、交操作 INTERSECT 和差操作 EXCEPT

```sql
SELECT *
FROM Student
WHERE Smajor = '计算机科学与技术'
UNION
SELECT * 
FROM Student
WHERE (extract(year from current_date) - extract(year from Sbirthdate)) <= 19;
```

### 3.3.5 基于派生表的查询

子查询不仅可以出现在WHERE子句中, 还可以出现在FROM子句中, 这时子查询生成的临时派生表成为主查询的查询对象.

找出每个学生超过他自己选修课程平均成绩的课程号

```sql
SELECT Sno, Cno
FROM SC, 
     (SELECT Sno, AVG(Grade) AS Avg_grade 
      FROM SC 
      GROUP BY Sno) AS Avg_SC(Avg_sno, Avg_grade)
WHERE SC.Sno = Avg_SC.Avg_sno 
  AND SC.Grade >= Avg_SC.Avg_grade;
```


## 3.4 数据更新


## 3.5 空值的处理

## 3.6 视图